<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <div class="field">
     <b>Номер группы</b>
     <br>
    <%= f.text_field :name %>
  </div>
  <div class="field">

     <b>Количество студентов в группе</b>
    <br>
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить", class: 'btn btn-mini btn-primary'  %>
  </div>
<% end %>
