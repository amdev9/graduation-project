
<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
    <b>Номер аудитории</b><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
     
     <b>Лабораторная</b><br>
    <%= f.check_box :lab %>
  </div>
  <div class="field">
     
     <b>Количество мест</b><br>
    <%= f.number_field :size %>
  </div>
  <div class="actions" >
    <%= f.submit "Добавить новую аудиторию", class: "btn btn-primary" %>
  </div>
<% end %>
